import React from "react";

export const ModalContent = () => {
	return (
		<div className="alert alert-warning text-center sticky-top" role="alert">
			No se ha podido añadir, porque ya está en favoritos.
		</div>
	);
};
